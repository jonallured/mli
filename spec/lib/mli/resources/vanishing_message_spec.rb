RSpec.describe Mli::VanishingMessage do
  describe ".create" do
    context "without required attrs" do
      it "posts attrs and returns bad request error"
    end

    context "with required attrs" do
      it "posts attrs and returns success"
    end
  end
end
